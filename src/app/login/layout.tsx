export const metadata = {
    title: "Sign In",
};
  
export default function SignIn({ children }: { children: React.ReactNode }) {
    return (
        <html lang="en">
          <body>
            {children}
          </body>
        </html>
    );
  }  
  